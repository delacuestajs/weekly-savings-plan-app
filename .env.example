# ===========================================
# Environment Variables Configuration
# ===========================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# NEVER commit .env to version control!
# ===========================================

# ----- Caddy (Reverse Proxy) -----
# Your domain or DDNS hostname for HTTPS
CADDY_DOMAIN=your-domain.com

# ----- Application -----
# Port for local LAN access (default: 9283)
APP_PORT=9283

# Default password for new users and password resets (default: abcd1234)
DEFAULT_PASSWORD=abcd1234

# App version and build info (auto-generated during deployment)
APP_VERSION=0.9.0
APP_BUILD_DATE=2026-06-27 03:23:39

# ----- Database (MySQL) -----
# Database host (use 'db' for Docker, 'localhost' for local)
DB_HOST=db

# Database name
DB_NAME=savings_db

# Database user credentials
DB_USERNAME=root
DB_PASSWORD=your_db_password

# MySQL root password (for initial setup)
DB_ROOT_PASSWORD=your_root_password
